Command line instructions

Git global setup
git config --global user.name "zhaoyulong"
git config --global user.email "zhaoyulong@smzdm.com"

Create a new repository
git clone https://gitlab-team.smzdm.com/smzdm/dw_realtime.git
cd dw_realtime
touch README.md
git add README.md
git commit -m "init"
git push -u origin master

Existing folder or Git repository
cd existing_folder
git init
git remote add origin https://gitlab-team.smzdm.com/smzdm/dw_realtime.git
git remote add origin https://gitlab-team.smzdm.com/smzdm/dw_batch.git

git clone https://gitlab-team.smzdm.com/smzdm/dw_batch.git
git pull origin master

git add .
git commit -m "init"
git push -u origin master


git pull --rebase origin master
echo "[credential]" >> .git/config
echo "    helper = store" >> .git/config
git config --list
--------------------------------------
--清除大文件
git filter-branch --force --index-filter "git rm --cached --ignore-unmatch 'data_warehouse/ga/jar/*'"  --prune-empty --tag-name-filter cat -- --all
git push --force --all
rm -rf .git/refs/original/
git reflog expire --expire=now --all
git gc --prune=now



git init
git remote add origin  https://gitlab-team.smzdm.com/smzdm/dw_batch.git
git config core.sparsecheckout true
echo "/data_warehouse/*" > .git/info/sparse-checkout
git pull origin master



#!/bin/bash

cd /data/source

git pull --rebase origin master

sh /data/gitupdate.sh


gitk -- <file>
